# Development + test dependencies - not installed in the production image.
# Pin with: make lock -> generates requirements-dev.txt
# Do not install from this file in CI - use requirements-dev.txt instead.
-r requirements.in
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27
ruff>=0.5
mypy>=1.10
anyio[trio]>=4.4
# Dependency locking is done with `uv pip compile` (see `make lock`), which is
# installed as a standalone tool, not a project dependency. pip-tools was removed
# here because `make lock` no longer uses it.
